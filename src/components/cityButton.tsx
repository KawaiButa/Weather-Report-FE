"use client"
import { Button } from "@mui/material";

const CityButton = () => {
    return (
        <Button variant="contained" size="large">
            Search
        </Button>
    );
}

export default CityButton;